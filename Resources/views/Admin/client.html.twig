{# on inclue le template de la vue clients correspondant à la subSection #}
{% if subSection is defined %}
    {% include "MesClicsEspaceClientBundle:Admin:client-" ~ subSection ~ ".html.twig" %}
{% endif %}